---
version: '3'
services:
  redis:
    image: redis:alpine
    volumes:
    - 'redis:/data'
  server:
    build: ./
    image: scrumblr
    links:
    - redis
    ports:
    - '7000:8080'
volumes:
  redis:

